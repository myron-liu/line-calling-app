import { TeamsList } from "@/components/setup/teams-list";

// Teams list / home (§16, §4.1) — only the teams the signed-in phone manages.
export default function TeamsPage() {
  return <TeamsList />;
}
